# SG Micro System Solutions (Placeholder)

> **Status:** Phase 5 placeholder. No content yet.
> **Scope:** SG Micro-published reference designs, evaluation kits, SDK
> examples, and application notes — all sourced from `sg-micro.com`. The
> BOM in each file contains **only SG Micro parts** (this directory is the
> single-vendor counterpart to the multi-vendor `application-solution/`).

## Planned Topics

| Solution | Type | Hero part(s) | Source |
|----------|------|--------------|--------|
| SGM3157 sensor MUX reference | Reference design | SGM3157 | sg-micro.com (verification pending) |
| SGM8903 audio output reference | Reference design | SGM8903 | sg-micro.com (verification pending) |
| SGM358 sensor amplifier reference | Reference design | SGM358 | sg-micro.com (verification pending) |

## Adding a New Solution

For each SG Micro-published solution, create a Markdown file named
`<topic>.md` with the standard template. Use `specs/SGMicro/<Part>.yaml`
for hero part metadata.
